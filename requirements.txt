numpy>=1.20.0
Pillow>=10.0.0
pystray>=0.19.0
# Web search — try duckduckgo_search first (stable, no Rust deps);
# web_tools.py will also accept the renamed 'ddgs' package as a fallback.
duckduckgo_search>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
psutil>=5.9.0
lxml>=5.0.0
