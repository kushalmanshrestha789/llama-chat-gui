# Validation Report — 2026-07-03 (Linux)

> Generated by running [`AI_VALIDATION_PROMPT.md`](../../AI_VALIDATION_PROMPT.md) against the repository at `8efbbb16d1fe7d3ee0e83b2d1b2bb5142b189578` (main HEAD at the time of the run).
>
> **Verdict:** PARTIAL_PASS — code is healthy, two dependency-install failures are environmental (PyPI download timeout in a headless container, not a code defect).

## Summary

| Field | Value |
|---|---|
| Repo | https://github.com/kushalmanshrestha789/llama-chat-gui |
| Commit | `8efbbb16d1fe7d3ee0e83b2d1b2bb5142b189578` (main HEAD) |
| OS | Linux (headless container) |
| Python | 3.12.12 |
| Tk | 8.6.13 available (GUI skipped — no `$DISPLAY`) |
| Verdict | **PARTIAL_PASS** |

## Static checks

| Check | Result |
|---|---|
| `py_compile platform_compat.py` | ✅ PASS |
| `py_compile llama_controller.py` | ✅ PASS |
| `py_compile web_tools.py` | ✅ PASS |
| `py_compile llama_gui.py` | ⏭ Skipped (intentionally — imports `tkinter` and `psutil`, tested under "Runtime imports" below) |

## `platform_compat` self-test

```
IS_WINDOWS = False
IS_LINUX   = True
config_dir()  = /home/kimi/.config
data_dir()    = /home/kimi/.local/share
documents_dir = /home/kimi/Documents
models_root() = /home/kimi/.lmstudio/models
history_dir() = /home/kimi/Documents/ChatArchive/llama_chat_project/history
benchmark_dir() = /home/kimi/.local/share/llama-chat/benchmarks
nvidia_smi_cmd() = ['nvidia-smi']
subprocess_no_window_kwargs() = {}
is_dark_mode() = None
```

> Note: validator ran as user `kimi`, not `cipher` — `config_dir() → /home/kimi/.config` confirms the helper correctly resolves `Path.home()` at runtime, not a hardcoded path. ✅

## Cross-platform regression assertion

```
REGRESSION_OK
```

All Linux expectations hold (config_dir, data_dir, models_root, nvidia-smi, subprocess flags).

## Dependency install

| Package | Result |
|---|---|
| `ddgs` | ❌ Failed — `primp` wheel download timed out (PyPI network issue, not a code issue) |
| `requests` | ✅ Installed (2.32.3, already present) |
| `beautifulsoup4` | ✅ Installed (4.15.0, already present) |
| `psutil` | ✅ Installed (7.0.0, already present) |
| `Pillow` | ✅ Installed (12.2.0, already present) |
| `pystray` | ❌ Failed — pip rolled back the whole transaction on the `ddgs`/`primp` timeout. Wheel was cached (49 kB) but never installed. |
| `numpy` (2.2.5) | ✅ Installed (transitive, not individually tested) |
| `lxml` (6.0.1) | ⚠ Listed as installed but not importable — see Notes |

**Root cause of failures:** The `ddgs` package depends on `primp>=1.2.3`, a 5.3 MB Rust-based binary wheel. The download from `files.pythonhosted.org` timed out twice in the headless container (60s and 300s). pip's transactional install then rolled back the entire operation, which is why `pystray` (which had already been cached successfully) was never installed either.

**Resolution in `666ecfc`:** The `web_tools.py` fallback was reversed to prefer `duckduckgo_search` (no `primp` dependency) over `ddgs`, and `requirements.txt` was updated to match. Re-running on a machine with normal PyPI connectivity is expected to push this from `PARTIAL_PASS` to `PASS`.

## `llama-cli`

❌ **LLAMA_CLI_MISSING** — `sh: 1: llama-cli: not found`, exit code 127.

**Expected:** The validation prompt explicitly says not to install `llama-cli`. This is a clean container without llama.cpp. The application code is not affected.

## Runtime imports

| Import | Result |
|---|---|
| `import platform_compat` | ✅ PASS |
| `import web_tools` | ✅ PASS |
| `import llama_controller` | ✅ PASS |
| `import llama_gui` | ❌ FAIL — `ModuleNotFoundError: No module named 'pystray'` (cascading failure from the install rollback) |

## GUI smoke (optional)

⏭ **HEADLESS_SKIP** — No `$DISPLAY` environment variable; this is a headless container.

## Notes

- **No deprecation warnings** were emitted during any of the Python static checks, self-test, or regression assertion runs.
- The `lxml` package is listed by pip as installed (6.0.1) but is not importable in the test environment — likely a pre-existing install issue on this container. Not caused by the project.
- `platform_compat.py` paths look correct against XDG expectations on Linux.
- The `pystray` cascading failure illustrates a real-world pip install pattern: a single timeout in one dependency's wheel can roll back the whole `pip install -r requirements.txt` run. The fix in `666ecfc` (preferring `duckduckgo_search`, which has no `primp` dependency) is the structural answer to this class of problem.

## Action items

- [x] **Fix the `ddgs` → `primp` fragility** → `666ecfc fix: prefer duckduckgo_search over ddgs in web search fallback`
- [ ] **Re-run on a machine with normal PyPI connectivity** to confirm full PASS (the validator that produced this report was headless + slow network)
- [ ] **Re-run on Windows** to validate the cross-platform paths (Windows-specific assertions couldn't be exercised in a Linux container)

## How to reproduce

1. Open any capable AI assistant in a clean environment
2. Paste the prompt from [`AI_VALIDATION_PROMPT.md`](../../AI_VALIDATION_PROMPT.md)
3. (Optionally) append: "Check out commit `8efbbb1` for this run."
4. Save the AI's response to `docs/validation/<date>-<os>-<verdict>.md`
